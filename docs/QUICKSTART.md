# Quickstart

```bash
git clone https://github.com/OGAitherTech/AitherBackend.git
cd AitherBackend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
fastapi dev app/main.py
```

Then open `/docs` for the interactive API explorer.
