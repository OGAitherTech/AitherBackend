# Versioning

AitherBackend starts at `1.0.0`.

The public API version is exposed by `GET /api/version` and the root endpoint. Breaking API contract changes should use a deliberate major-version strategy rather than silently changing existing responses.
