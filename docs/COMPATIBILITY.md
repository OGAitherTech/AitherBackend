# Compatibility

The backend targets Python 3.12+ and exposes JSON/HTTP APIs suitable for web, iPhone/mobile, and desktop Aither clients.

Client applications should rely on documented endpoint contracts rather than internal Python modules.
