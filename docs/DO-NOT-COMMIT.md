# Never commit

- API keys
- Access tokens
- Passwords
- Private certificates
- Database credentials
- Provider secrets

Use `.env` locally and your deployment platform's secret manager in production.
