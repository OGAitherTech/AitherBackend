# Health endpoints

- `GET /healthz` — lightweight deployment/platform probe.
- `GET /api/health` — application health with timestamp.
- `GET /api/health/dependencies` — dependency readiness foundation.
