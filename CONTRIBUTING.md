# Contributing to AitherBackend

1. Create a feature branch from `main`.
2. Keep API keys and secrets out of Git.
3. Add tests for new endpoints and behavior.
4. Run `pytest -q` before opening a pull request.
5. Keep API responses backward-compatible when possible.
